import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const BookingExpired = () => {
  return (
    <View style = {{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center'
    }}>
      <Text>BookingExpired</Text>
    </View>
  )
}

export default BookingExpired

const styles = StyleSheet.create({})